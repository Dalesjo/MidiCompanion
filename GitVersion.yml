# GitVersion.yml
mode: ContinuousDeployment
branches:
  master:
    increment: Inherit
    regex: master
    source-branches: ['release', 'hotfix', 'support']
    is-release-branch: false
ignore:
  sha: [] 